# Colab GPU runtime: preserve the image's preinstalled NumPy/SciPy/Torch stack.
# CI/local use requirements/constraints.txt. These are minimum compatibility
# bounds only; install does not force a NumPy 1.x downgrade.
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
PyYAML>=6.0,<7
networkx>=3.1,<4
pytest>=7.4,<9
POT>=0.9.4,<0.10
